node_modules/
dist/
fixtures/_shared/node_modules/
.benchmark-cache/
.DS_Store
*.log
*.tgz
.claude/

# Local secrets (API keys for the paid LLM layers/benchmark) — never commit
.env
.env.*
!.env.example

# Ralph loop fresh-context run logs (local only; loop state lives under gitignored .claude/)
scripts/ralph/runs/
